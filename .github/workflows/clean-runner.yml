name: Clean runner

on:
  workflow_dispatch:

jobs:
  clean:
    name: Clean self-hosted runner
    runs-on: self-hosted
    steps:
      - name: Clean Julia depot
        run: rm -rf ~/.julia/packages ~/.julia/compiled ~/.julia/logs
      - name: Clean workspace
        run: rm -rf $GITHUB_WORKSPACE
